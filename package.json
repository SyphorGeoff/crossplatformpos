{
  "name": "aireus-pos",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Aireus POS EDGE — cross-platform POS client (web / Android / iOS). Protocol parity with the iOS Objective-C ISIS client.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "~5.8.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
